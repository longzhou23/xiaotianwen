# Fake OneBot (P1)

P0 produces OneBot-like synthetic event dictionaries in the replay engine.
This directory is reserved for a P1 loopback service that can model reconnect,
duplicate event, image, quote and merged-forward behavior without a QQ login.
