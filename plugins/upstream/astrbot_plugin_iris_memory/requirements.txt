# 向量索引
faiss-cpu>=1.7.0
numpy>=1.24.0

# 嵌入模型（可选，仅当 l2_memory.embedding_source 为 local 时需要）
# sentence-transformers>=2.2.0

# Token 计数
tiktoken>=0.5.0

# Web 框架
quart>=0.19.0

# 图片处理（pHash 去重 + 无效图过滤）
Pillow>=10.0.0

# HTTP 客户端（图片 URL 可达性检查）
httpx>=0.24.0
