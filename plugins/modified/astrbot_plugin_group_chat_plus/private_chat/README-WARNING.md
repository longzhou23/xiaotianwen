# ⚠️ 别看这里，这里没什么好看的

这个目录下的所有代码和文件——包括这个目录本身、以及 `private_chat_utils/` 等所有子目录里的每一个文件——目前都**没有任何实际作用和价值**，纯粹是一坨。

私聊功能只是暂时打了一个小口子，把未来可能会用到的功能模块的代码文件从群聊那边复制了一份过来，搭了个大概的目录骨架。但内部逻辑基本上还是群聊的内容，**根本没有接通**，实际上处于**完全不可用**的状态。

这里就是一个占位的底子，仅此而已。别花时间读这些文件，看到什么奇怪的、对不上的、跑不起来的——都是正常的，因为它们本来就不是给你看的。

**想看真正的代码？去看群聊部分。** 那才是正经东西。这边？吐口唾沫都嫌浪费。
