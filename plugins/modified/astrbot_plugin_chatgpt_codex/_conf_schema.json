{
  "codex_path": {
    "type": "string",
    "description": "Codex 可执行文件路径或命令名，例如 codex；如果不在 PATH 中，请填写绝对路径。",
    "default": "codex"
  },
  "backend_mode": {
    "type": "string",
    "options": ["transport", "app_server", "auto"],
    "description": "推理后端：transport（推荐）仅使用 Codex Responses HTTP/SSE；app_server 使用稳定的 Codex Agent Loop；auto 先尝试 transport，失败后回退 app_server。新配置默认使用 transport。",
    "default": "transport"
  },
  "transport_proxy": {
    "type": "string",
    "description": "推理与 ChatGPT OAuth / Device Code 登录共用的网络代理，例如 http://127.0.0.1:7890。Docker 中必须填写容器可访问的地址，不能使用宿主机的 127.0.0.1；不要填写用户名或密码。",
    "default": ""
  },
  "use_system_proxy": {
    "type": "bool",
    "description": "继承 AstrBot 进程的 HTTP_PROXY、HTTPS_PROXY、ALL_PROXY 系统代理；显式 transport_proxy 优先。Docker 需要在容器环境中设置这些变量。",
    "default": true
  },
  "login_mode": {
    "type": "string",
    "description": "ChatGPT 登录方式：浏览器 OAuth 或 Device Code。",
    "options": ["browser", "device_code"],
    "default": "browser"
  },
  "default_model": {
    "type": "string",
    "description": "服务端 model/list 返回的模型 ID；auto 表示使用第一个可见模型。",
    "default": "auto"
  },
  "reasoning_effort": {
    "type": "string",
    "description": "使用服务端 model/list 声明的推理强度；auto 表示交由服务端选择。",
    "default": "auto"
  },
  "route_reasoning_effort": {
    "type": "object",
    "description": "按 agent/chat/decision/proactive/vision/background 路由覆盖推理强度；未填写时使用全局 reasoning_effort。",
    "items": {},
    "default": {}
  },
  "route_max_output_tokens": {
    "type": "object",
    "description": "按路由限制最大输出 token（256-32768）；未填写表示不额外限制。",
    "items": {},
    "default": {}
  },
  "max_tool_result_chars": {
    "type": "int",
    "description": "工具结果注入上下文的最大字符数；保留首尾并插入截断标记，0 表示不截断。",
    "minimum": 0,
    "maximum": 50000,
    "default": 12000
  },
  "max_transport_images": {
    "type": "int",
    "description": "单次 transport 请求最多发送的最新图片数；0 表示不限制。",
    "minimum": 0,
    "maximum": 8,
    "default": 1
  },
  "harness_mode": {
    "type": "string",
    "options": ["lightweight", "codex"],
    "description": "Codex Agent 外壳：lightweight 只保留聊天所需指令，codex 保留原生 coding-agent 指令。",
    "default": "lightweight"
  },
  "tool_router": {
    "type": "string",
    "options": ["none", "minimal", "all"],
    "description": "AstrBot 工具路由：闲聊默认 minimal，当前 MVP 没有注册本地工具。",
    "default": "minimal"
  },
  "streaming": {
    "type": "bool",
    "description": "在 AstrBot 支持时使用流式 Provider 调用。",
    "default": true
  },
  "show_tool_status": {
    "type": "bool",
    "description": "显示安全的公开工具状态标签；不会显示命令、原始工具输出或隐藏思维链。",
    "default": false
  },
  "max_concurrent_turns": {
    "type": "int",
    "description": "所有会话同时运行的 Codex turn 最大数量。",
    "minimum": 1,
    "maximum": 32,
    "default": 2
  },
  "turn_timeout": {
    "type": "int",
    "description": "单个 turn 的超时时间（秒）；配额耗尽不会无限重试。",
    "minimum": 30,
    "maximum": 3600,
    "default": 600
  },
  "max_thread_turns": {
    "type": "int",
    "description": "单个 Codex thread 保留的最大已完成 turn 数；0 表示关闭按 turn 轮换。",
    "minimum": 0,
    "maximum": 10000,
    "default": 100
  },
  "thread_idle_ttl": {
    "type": "int",
    "description": "持久化 Codex thread 空闲多少秒后轮换。",
    "minimum": 0,
    "maximum": 31536000,
    "default": 604800
  },
  "thread_max_age": {
    "type": "int",
    "description": "持久化 Codex thread 存在多少秒后轮换。",
    "minimum": 0,
    "maximum": 31536000,
    "default": 2592000
  },
  "force_http_transport": {
    "type": "bool",
    "description": "强制 Codex ChatGPT 流量使用 HTTPS，避免 WebSocket 回退造成反复延迟。",
    "default": true
  },
  "enable_local_codex_tools": {
    "type": "bool",
    "description": "危险选项：允许 Codex 使用本机工具；安全默认值为关闭。",
    "default": false
  },
  "usage_timezone": {
    "type": "string",
    "description": "Usage 自然日使用的 IANA 时区，例如 Asia/Shanghai。",
    "default": "Asia/Shanghai"
  },
  "usage_retention_days": {
    "type": "int",
    "description": "本地 Usage 记录保留天数；0 表示永久保留。每天最多自动清理一次。",
    "minimum": 0,
    "maximum": 3650,
    "default": 365
  },
  "usage_debug": {
    "type": "bool",
    "description": "记录脱敏的数值型 Usage 诊断，便于排查累计值、重复事件和计数器重置；不会记录提示词、回复或令牌。",
    "default": false
  }
}
