# 插件清单

每个插件目录内的 `plugin.meta.yaml` 记录来源、许可证提示和本地修改状态。

- `upstream`：当前没有完成差异审计，不能直接声称与上游完全一致；
- `modified`：已知存在本地修改、二次开发或自研代码；
- `retired`：已停用但保留用于迁移追溯，不应被默认部署脚本安装。

首次公开推送前，应为每个 `upstream-diff-required` 项目补充上游 commit、差异报告和许可证确认。
