# Optional host-only registry override. Copy this to
# ${PROJECT_ROOT}/host-images.env when Docker Hub is unreachable.
#
# The images still track `latest`; only the registry route changes.
ASTRBOT_IMAGE=m.daocloud.io/docker.io/soulter/astrbot:latest
SNOWLUMA_IMAGE=docker.1ms.run/motricseven7/snowluma:latest
