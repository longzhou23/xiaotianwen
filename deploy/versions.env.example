# New machines and explicit deployment updates follow upstream latest images.
# Normal container restarts do not pull and therefore do not cause surprise
# upgrades. `record-image-digests.sh` records the exact pulled digests.
ASTRBOT_IMAGE=soulter/astrbot:latest
SNOWLUMA_IMAGE=motricseven7/snowluma:latest
