# P2 performance tests

Performance measurements are not a P0 correctness signal.  P2 will put
repeatable warm-up, P50/P95 and token/context-budget scenarios here.  Until
then, the P0 reports preserve structural call counts only and never label a
single local duration as a performance regression.
