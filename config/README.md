# 配置模板

这里仅保存可以公开展示的配置结构和环境变量名称。真实 AstrBot 配置、插件配置、QQ 登录数据、Cloudflare 凭证和 API Key 位于 private 实例仓库之外，通过部署时注入。

如果某个配置包含密码、Token、API Key、Cookie、证书或私钥，即使 private 仓库是私有的，也不要提交明文；请使用 `.env.example` 中的变量名或单独的加密备份。
